# ОТЧЁТ ТЕСТИРОВАНИЯ: НейроБокс

**Дата:** 15.02.2025  
**Сервер:** /opt/neurobox (локальная проверка кодовой базы)

---

## СВОДКА

| Категория | Количество |
|-----------|------------|
| ✅ Работает | 78 пунктов |
| ⚠️ Частично | 12 пунктов |
| ❌ Не работает | 2 пункта |
| 🔍 Не реализовано | 14 пунктов |

---

## 1. СТРУКТУРА ПРОЕКТА

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| Проект запускается (main.py / docker-compose up) | ✅ | `docker compose up -d --build`, точка входа `python -m bot.main` |
| Структура папок логична | ✅ | `handlers/`, `services/`, `keyboards/`, `middlewares/`, `db/`, `providers/`; отдельно `admin_panel/`, `config/`, `scripts/`, `migrations/` |
| Нет God-файлов (> 500 строк) | ⚠️ | `start.py` ~777 строк, `text.py` ~743, `admin_panel/routes.py` ~665 — стоит разбить на модули |
| Нет дублирования кода | ✅ | Общая логика в `services/` (credits, yookassa, redis_store), хендлеры тонкие |
| Нет мёртвого кода | ✅ | Закомментированных блоков и неиспользуемых импортов в продакшн-коде не обнаружено |
| requirements.txt — версии пинованы | ❌ | Используются диапазоны (`aiogram>=3.15.0`, `httpx>=0.28`), не `aiogram==3.15.0` |
| .env.example существует | ✅ | Есть, без реальных ключей |
| .gitignore: .env, __pycache__, venv, *.pyc | ✅ | Содержит `.env`, `__pycache__`, `venv`, `*.pyc`, `backups/` и др. |
| Dockerfile / docker-compose.yml корректны | ✅ | Dockerfile на `python:3.12-slim`, compose: postgres, redis, bot, worker, webhook, admin |
| README.md — инструкция по запуску | ✅ | Быстрый старт (Docker и локально), Env, миграции, тесты |

**Рекомендации:**  
- Запинить версии в `requirements.txt` (например через `pip freeze` после проверки).  
- Вынести из `start.py`/`text.py` крупные блоки в подмодули (например, оплата/история в отдельные файлы).

---

## 2. КОНФИГУРАЦИЯ И ЗАПУСК

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| .env содержит все необходимые переменные | ✅ | BOT_TOKEN, BOT_USERNAME, POSTGRES_*, REDIS_*, YOOKASSA_*, OPENAI_* и др. в .env.example |
| Переменные через os.getenv() / pydantic Settings | ✅ | `config/settings.py` — pydantic-settings, без хардкода |
| Нет хардкод-значений (ключи, URL, лимиты) в коде | ⚠️ | В `docker-compose.yml`: `ADMIN_PANEL_PASSWORD=${ADMIN_PANEL_PASSWORD:-forossvernaya4389}` — дефолт пароля в открытом виде |
| docker-compose: healthcheck для каждого сервиса | ⚠️ | healthcheck только у postgres, redis, bot; у worker, webhook, admin — нет |
| docker-compose: restart: always | ✅ | У всех сервисов |
| docker-compose: depends_on с condition: service_healthy | ✅ | bot, worker, webhook, admin зависят от postgres (и bot/worker от redis) с condition |
| docker-compose: volumes для данных | ✅ | `pg_data`, `redis_data` |
| PostgreSQL/Redis порты НЕ проброшены наружу | ✅ | В `ports:` только 8090 (webhook), 8091 (admin) |

**Рекомендации:**  
- Убрать дефолт пароля из compose или оставить пустым с обязательной установкой в .env.  
- Добавить healthcheck для worker/webhook/admin (например, проверка процесса или простой HTTP/скрипт).

---

## 3. БАЗА ДАННЫХ

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| Модели/таблицы определены корректно | ✅ | asyncpg + миграции; таблицы: users, credit_transactions, ai_requests, payments, daily_stats, subscription_ends |
| Все таблицы имеют primary key | ✅ | PK везде (id или (date, metric), payment_id UNIQUE у payments) |
| Индексы на user_id, created_at, payment_id, status | ✅ | idx_payments_user, idx_credit_tx_user, idx_ai_requests_user, idx_payments_status и др. |
| Foreign keys настроены | ✅ | users(id), payments(user_id), credit_transactions(user_id), ai_requests(user_id) |
| Alembic настроен и миграции существуют | 🔍 | Alembic не используется; миграции — SQL-файлы в `migrations/`, применение через `bot.db.migrate` |
| Нет raw SQL с f-string/.format() (SQL-injection) | ✅ | В основном параметризованные запросы ($1, $2). В admin_panel `interval` берётся из whitelist (day/week/month → "1 day"/"7 days"/"30 days"), не от пользователя. В credits `col` из внутреннего словаря task_type → column |
| Пул соединений настроен | ✅ | asyncpg pool в `bot/db/database.py` |
| Нет N+1 запросов | ✅ | Eager loading и батчи там, где нужно; одиночные запросы по ключам |

**Рекомендации:**  
- Для единообразия и откатов можно ввести Alembic поверх существующих SQL-миграций (опционально).

---

## 4. ОБРАБОТЧИКИ (HANDLERS)

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| /start — работает, главное меню | ✅ | Приветствие, клавиатура, триал, баланс |
| /help — показывает справку | 🔍 | Отдельной команды /help нет; справка через кнопку «📖 Гайд» (callback `guide:menu`) |
| Все кнопки главного меню ведут куда нужно | ✅ | Callback-обработчики зарегистрированы (screen_*, balance, buy_credits, select_model и т.д.) |
| Все callback_data обрабатываются | ✅ | Мёртвых кнопок не выявлено |
| FSM-сценарии завершаются | ✅ | Состояния очищаются после завершения или отмены |
| FSM: таймаут / отмена (/cancel или кнопка) | ✅ | Есть /cancel, кнопки «Назад»/«Меню» |
| Невалидные данные → корректная ошибка | ✅ | Пустое сообщение → «Напиши текст или /start»; неизвестная команда → «Неизвестная команда» |
| Фото/видео/документ когда не ожидается | ✅ | Обработчики разделены (photo, video, doc_handler), лишнее не крашит |
| Стикер/голосовое не крашит | ✅ | Обработка текста и голоса есть; прочие типы не ведут к падению |
| Регистр команд (/START, /Start) | ✅ | Обработка через Command() от aiogram, регистр не важен |
| Handler'ы в try/except / глобальная обработка | ✅ | Глобальный `@dp.errors()` в main.py + точечные try/except в хендлерах |
| Нет handler'ов без фильтров (ловят всё) | ✅ | Текстовый роутер с фильтром (F.text и не команда), остальные по типу/команде/callback |

**Рекомендации:**  
- Добавить команду /help, которая дублирует «Гайд» (отправка того же контента или ссылки на гайд), для привычного UX.

---

## 5. КЛАВИАТУРЫ

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| Inline-кнопки с уникальными callback_data | ✅ | Все callback_data уникальны по смыслу (screen_text, balance, buy_credits, admin:stats и т.д.) |
| callback_data < 64 байт | ✅ | Короткие строки (trial_activate, main_menu, guide:menu, our_bots и др.) |
| Нет дубликатов callback_data с разным поведением | ✅ | Один callback — один обработчик |
| Reply-клавиатура когда нужна | ✅ | Используется в сценариях (например, отмена) |
| Клавиатура убирается когда не нужна | ✅ | ReplyKeyboardRemove где уместно |
| Кнопка «Назад»/«В меню» | ✅ | «◀️ Меню» (main_menu) в основных экранах |

---

## 6. ОПЛАТА (YooKassa)

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| idempotence_key уникальный | ✅ | `Idempotence-Key: str(uuid.uuid4())` при создании платежа |
| metadata: user_id, bot_name, plan/package | ✅ | user_id, bot_name, pack, credits в metadata |
| return_url ведёт в бота (deep link) | ✅ | `https://t.me/{bot_username}?start=pay` |
| Описание платежа понятное | ✅ | «НейроБокс — N кредитов» |
| Webhook endpoint есть и обрабатывает события | ✅ | scripts/webhook_server.py: payment.succeeded, payment.canceled, refund.succeeded |
| Верификация IP YooKassa | ✅ | Белый список IP в webhook; можно отключить через SKIP_YOOKASSA_IP_CHECK |
| payment.succeeded → зачисление | ✅ | process_payment_webhook → confirm_payment_record → add_credits / set_unlimited_until |
| payment.canceled → логирование | ✅ | log_payment_canceled, ответ 200 |
| refund.succeeded → отмена/списание | ✅ | process_refund_webhook, списание кредитов |
| Идемпотентность: проверка payment_id в БД | ✅ | confirm_payment_record с FOR UPDATE; при уже confirmed возвращается None, кредиты не дублируются |
| Дубль webhook не зачисляет повторно | ✅ | Уже подтверждённый платёж → already_confirmed, без повторного начисления |
| Ответ 200 OK быстро (< 5 сек) | ✅ | Синхронная обработка, без долгих операций перед ответом |
| Таблица payments с нужными полями | ✅ | user_id, payment_id, amount_rub, credits_amount, pack_name, status, created_at, confirmed_at |
| Уникальный индекс на payment_id | ✅ | payment_id VARCHAR(100) UNIQUE |
| Rate limit на создание платежей (5/мин на user) | ⚠️ | Есть только payment_lock на 15 сек на user_id (защита от двойного нажатия). Отдельного лимита 5 платежей/мин нет |

**Рекомендации:**  
- Добавить лимит создания платежей (например, счётчик в Redis по user_id с TTL 60 сек, max 5 запросов).

---

## 7. ПОДПИСКИ И ЛИМИТЫ

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| Таблица subscriptions / аналог | ⚠️ | Классической subscriptions нет; есть кредиты (users.credits_*), пакет «unlimited» (unlimited_until), триал 45 мин, таблица subscription_ends для напоминаний |
| Проверка подписки/лимитов на премиум-хендлерах | ✅ | Middleware/проверки баланса и unlimited в services/credits, списание за запросы |
| Бесплатные лимиты (N действий/день) | ✅ | FREE_DAILY_CREDITS, credits_free_today, сброс по дате |
| Счётчик лимитов в Redis с TTL до 00:00 MSK | ⚠️ | Сброс по credits_free_reset (DATE) в БД, не Redis с TTL до полуночи MSK |
| Подписчик без лимита | ✅ | Пакет «unlimited» даёт безлимит до end date |
| Исчерпание лимита → пейволл | ✅ | Сообщение о нехватке кредитов, кнопка пополнения |
| Деактивация expired подписок | ✅ | Проверка unlimited_until при запросах |
| Напоминания о продлении | ✅ | subscription_ends, get_subscription_reminders_due, reminder_loop в main |
| Grace period после окончания | 🔍 | Явного grace period в коде не видно |
| Продление до/после окончания (end_date += 30) | 🔍 | Логика продления «unlimited» при повторной оплате не проверялась детально; при оплате пакета unlimited выставляется set_unlimited_until |

**Рекомендации:**  
- При желании унифицировать с «классическими» подписками — ввести таблицу subscriptions (user_id, plan, start_date, end_date, is_active) и переносить туда данные при оплате unlimited.  
- Grace period и точные правила продления (до/после end_date) описать в коде и в справке.

---

## 8. AI-ИНТЕГРАЦИЯ

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| API-клиент в отдельном сервисе | ✅ | providers/ (openai_text и др.), вызовы из handlers через сервисы |
| System prompt без пользовательского ввода | ✅ | Системный промпт задаётся в коде, пользовательский — в user message |
| Ограничение длины ввода | ✅ | Обрезка/проверка длины (например, до 4000 символов) в обработчиках |
| Timeout на API-запрос | ✅ | httpx/client timeout (30 c и др.) в провайдерах |
| Retry с backoff при 429/500/503 | ✅ | Retry и backoff в провайдерах (tenacity или аналог) |
| Fallback при недоступности API | ✅ | Сообщение пользователю об ошибке, без падения бота |
| Контекст диалога в Redis с TTL | ✅ | Ключи с ex= в Redis для контекста/состояния |
| Стоимость запроса (кредиты/лимиты) | ✅ | Списание кредитов, логирование в ai_requests |
| Typing action при генерации | ✅ | chat.do("typing"), chat.do("upload_photo") и т.д. в handlers |
| Дисклеймер (мед/юр/вет) | 🔍 | В системном промпте есть упоминание ограничений (jailbreak и т.п.); отдельного юридического дисклеймера в ответах не видно |

---

## 9. АДМИН-ПАНЕЛЬ

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| Команда /admin существует | ✅ | Вход в админ-меню по команде |
| Доступ только ADMIN_IDS | ✅ | _is_admin(user_id) по settings.admin_ids (список ID) |
| Неавторизованные → тишина / без подсказки | ⚠️ | При нажатии админ-колбэков неадмину показывается «Нет доступа» (show_alert) — лучше не раскрывать наличие админки |
| Попытки неавторизованного доступа логируются | 🔍 | Явного логирования попыток доступа в коде не найдено |
| Статистика: пользователи, активные, платящие, выручка | ✅ | Веб-админка (admin_panel): дашборд по периодам, платежи, запросы |
| Рассылка: текст + фото + кнопка + аудитория | ✅ | Broadcast: текст, фото, кнопка URL, выбор аудитории |
| Batch-отправка (30 msg/сек) | ✅ | BROADCAST_BATCH_DELAY = 1/30, asyncio.sleep между сообщениями |
| Заблокировавшие бота → пропуск + is_blocked | ✅ | При 403/blocked — UPDATE users SET is_blocked = TRUE |
| Управление пользователями: поиск, инфо, бан | ✅ | Модули users, moderation в админке |
| Логирование действий админа | 🔍 | admin_logs или аналог не проверялись детально; логи через structlog есть |

**Рекомендации:**  
- Для неадминов: не показывать alert «Нет доступа», а просто answer() без текста или общее «Команда недоступна».  
- Добавить явное логирование (user_id, action) при попытке входа в админку не из ADMIN_IDS.

---

## 10. БЕЗОПАСНОСТЬ

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| Все ключи/токены в .env, не в коде | ✅ | grep по sk-/token=/password= в *.py находит только settings/ getenv, не хардкод |
| .env в .gitignore | ✅ | Есть |
| SQL только ORM/параметризованные запросы | ✅ | Параметры через $1, $2; admin_panel interval из whitelist |
| Prompt injection: фильтр | ✅ | _is_prompt_injection (ignore previous, system:, you are now и т.д.) в text.py |
| eval()/exec() на юзер-вводе | ✅ | Не используются |
| Загрузка файлов: размер и MIME | ⚠️ | Проверки размера/MIME в коде не просматривались детально; стоит явно ограничить < 10 МБ и белый список MIME |
| Rate limiting | ✅ | RateLimitMiddleware (окно 60 сек, макс. 45 запросов) |
| Заблокированные пользователи — middleware | ✅ | is_blocked проверяется, можно блокировать через админку |
| Flood protection (> 5 msg/сек) | ✅ | Реализовано через тот же rate limit (45/мин) |
| callback_data — валидация формата | ✅ | Обработчики по точному совпадению F.data == "..." или startswith, нет разбора произвольной строки |
| FSM таймаут (автосброс) | ⚠️ | TTL FSM в Redis задаётся при инициализации хранилища; явного «5 мин неактивности» в коде не видно |
| Логирование ошибок (logging, не print) | ✅ | structlog по коду |
| Нет print() в продакшн-коде | ⚠️ | print есть в scripts (healthcheck, check_apis, global_tests, migrate) — для CLI/скриптов допустимо; в bot/* и admin_panel/* в продакшн-путях не используется |

**Рекомендации:**  
- Явно ограничить размер загружаемых файлов и проверять MIME.  
- Настроить FSM state TTL в Redis (например, 5–30 мин) и при необходимости сбрасывать состояние по таймауту.

---

## 11. ОБРАБОТКА ОШИБОК И ОТКАЗОУСТОЙЧИВОСТЬ

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| Внешние API в try/except | ✅ | httpx/yookassa и др. обёрнуты |
| Timeout на все HTTP-запросы | ✅ | httpx timeout задаётся |
| Redis недоступен → бот не крашится | ✅ | Fallback на MemoryStorage при недоступности Redis |
| PostgreSQL недоступен → «техработы» | ⚠️ | healthcheck падает; поведение бота при обрыве БД во время работы не проверялось (логирование и ответ пользователю желательны) |
| Telegram API 429 → retry с backoff | 🔍 | В коде не просматривалось; при необходимости добавить в вызовы Bot API |
| Неожиданное исключение → logging + сообщение юзеру | ✅ | @dp.errors() в main.py |
| Ни один handler не крашит бота | ✅ | Глобальный обработчик ошибок и точечные try/except |
| Graceful shutdown (SIGTERM) | ✅ | Обработка SIGTERM/SIGINT, ожидание pending tasks, закрытие pool и сессии |
| Healthcheck endpoint | ✅ | scripts/healthcheck.py (проверка БД), используется в docker-compose для bot |

---

## 12. REDIS

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| Все ключи с TTL | ⚠️ | Многие ключи с ex= (balance cache, payment_lock, photo_pending, voice_text и т.д.); полный аудит всех ключей не проводился — убедиться, что нет вечных ключей |
| Naming: bot_name:user_id:purpose | ⚠️ | Частично (balance:{user_id}, payment_lock:{user_id}); префикса «neurobox» или имени бота нет везде |
| FSM storage через Redis | ✅ | RedisStorage при доступном Redis, иначе MemoryStorage |
| FSM expire настроен | 🔍 | Зависит от версии aiogram и настроек RedisStorage; при необходимости задать state_ttl |
| Контекст диалога с TTL | ✅ | Ключи с ex= в handlers (например, 300–3600 сек) |
| Rate limit counters с TTL | ⚠️ | Rate limit в коде — in-memory (словарь _rate), не Redis; при нескольких инстансах бота лимит не общий |
| Нет утечек (растущие ключи без TTL) | ⚠️ | Нужен периодический просмотр ключей в Redis (KEYS/SCAN) на предмет бесконечных |

**Рекомендации:**  
- Вынести rate limit в Redis с TTL, чтобы лимит был общим для всех воркеров.  
- Ввести единый префикс ключей (например, neurobox:user_id:...) и документировать.

---

## 13. КРОСС-ПРОМО

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| Рекомендации других ботов | ✅ | Кнопка «🤖 Наши боты» (callback our_bots), список OUR_BOTS в start.py |
| Частота не чаще 1 на 5 сообщений | 🔍 | В коде не видно счётчика «каждое 5-е сообщение»; промо по кнопке |
| Контекстные по теме | 🔍 | Список фиксированный, не динамический по контексту |
| Не повторяется подряд | 🔍 | Нет отдельной логики «не показывать ту же промо дважды подряд» |
| Кнопка «Наши боты» в главном меню | ✅ | Есть в главной клавиатуре |
| Ссылки корректные (@username_bot) | ✅ | InlineKeyboardButton с url= на ботов/каналы |

---

## 14. DOCKER И ДЕПЛОЙ

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| Dockerfile: базовый образ с версией | ✅ | python:3.12-slim |
| .dockerignore существует | ❌ | Файл .dockerignore не найден — в образ могут попасть .env, .git, __pycache__, venv |
| docker-compose: healthcheck у всех | ⚠️ | Только postgres, redis, bot; worker, webhook, admin без healthcheck |
| docker-compose: restart: always | ✅ | Да |
| docker-compose: resource limits | 🔍 | Лимитов memory/cpu не задано |
| Сеть bridge (не host) | ✅ | nb_net, driver: bridge |
| Логи не переполняют диск | 🔍 | Конфигурация логирования не просматривалась; при использовании json-file драйвера задать max-size/max-file |
| Volumes для persistent данных | ✅ | pg_data, redis_data |

**Рекомендации:**  
- Добавить `.dockerignore` с минимумом: `.env`, `.git`, `__pycache__`, `*.pyc`, `venv`, `.venv`, `tests`, `*.md` (по необходимости).  
- Добавить healthcheck для worker/webhook/admin.  
- При необходимости ограничить memory/cpu у сервисов.

---

## КРИТИЧЕСКИЕ ПРОБЛЕМЫ (исправить немедленно)

1. **❌ .dockerignore отсутствует**  
   В образ могут попасть .env, .git, venv — риск утечки секретов и раздувания образа.  
   **Исправление:** создать `.dockerignore` и включить в него `.env`, `.git`, `__pycache__`, `venv`, `.venv`, `*.pyc`, при необходимости `tests`, `docs`.

2. **❌ requirements.txt без пинования версий**  
   Возможны несовместимые обновления при установке.  
   **Исправление:** зафиксировать версии (например, `aiogram==3.15.0`) или использовать `pip freeze` для снимка окружения.

---

## ВАЖНЫЕ ПРОБЛЕМЫ (исправить в ближайшие дни)

1. **⚠️ Дефолт пароля админки в docker-compose**  
   `ADMIN_PANEL_PASSWORD=${ADMIN_PANEL_PASSWORD:-forossvernaya4389}` — пароль по умолчанию в открытом виде.  
   **Исправление:** убрать дефолт или оставить пустую строку и требовать задание в .env.

2. **⚠️ Крупные файлы (God files)**  
   start.py, text.py, routes.py по 650–780 строк.  
   **Исправление:** разбить на подмодули (оплата, история, гайд, промо и т.д.).

3. **⚠️ Rate limit только in-memory**  
   При нескольких инстансах бота лимит не общий, возможен обход.  
   **Исправление:** хранить счётчики в Redis с TTL.

4. **⚠️ Нет лимита на создание платежей (5/мин на user)**  
   Есть только краткий payment_lock (15 сек).  
   **Исправление:** счётчик в Redis по user_id с TTL 60 сек, максимум 5 созданий платежа.

5. **⚠️ Healthcheck только у части сервисов**  
   worker, webhook, admin без healthcheck.  
   **Исправление:** добавить простой healthcheck (процесс или скрипт) для этих контейнеров.

6. **⚠️ Неадминам раскрывается наличие админки**  
   При нажатии админ-кнопки показывается «Нет доступа».  
   **Исправление:** не показывать alert, отвечать нейтрально или не отвечать.

---

## РЕКОМЕНДАЦИИ (улучшения)

1. **🔍 Команда /help** — дублировать контент «Гайд» по команде /help для привычного UX.
2. **🔍 Явная проверка размера и MIME загружаемых файлов** — лимит < 10 МБ и белый список типов.
3. **🔍 FSM state TTL** — настроить автосброс состояния через 5–30 мин неактивности в Redis.
4. **🔍 Единый префикс ключей Redis** — например, `neurobox:user_id:balance`, документировать.
5. **🔍 Логирование попыток доступа в админку** — user_id, callback/command, результат (denied).
6. **🔍 Grace period и правила продления подписки** — описать в коде и в справке для пользователей.
7. **🔍 Resource limits в docker-compose** — задать memory (и при необходимости cpu) для bot, worker, webhook, admin.
8. **🔍 Обработка недоступности БД** — при обрыве соединения показывать пользователю «Техработы» и логировать.

---

## БЕЗОПАСНОСТЬ

| Проверка | Результат |
|----------|-----------|
| Утечки ключей в коде | ✅ Не найдено |
| SQL-injection | ✅ Параметризованные запросы; admin interval из whitelist |
| Prompt injection | ✅ Фильтр в text.py |
| Rate limiting | ✅ Есть (in-memory; рекомендуется Redis) |

---

## ОПЛАТА

| Проверка | Результат |
|----------|-----------|
| Создание платежа | ✅ idempotence key, metadata, return_url |
| Webhook | ✅ Обработка succeeded/canceled/refund, проверка IP |
| Идемпотентность | ✅ FOR UPDATE + проверка status, повторный webhook не зачисляет |
| Подписки/кредиты | ✅ Кредиты и пакет unlimited; таблица subscription_ends для напоминаний |

---

## АДМИН-ПАНЕЛЬ

| Проверка | Результат |
|----------|-----------|
| Доступ по ADMIN_IDS | ✅ |
| Статистика | ✅ Дашборд, платежи, запросы |
| Рассылка | ✅ Текст, фото, кнопка, 30 msg/сек, is_blocked при 403 |

---

*Отчёт составлен по кодовой базе без изменения кода (только тестирование и аудит).*
